apiVersion: apps/v1
kind: Deployment
metadata:
  name: base-commerce
  namespace: load-test
spec:
  replicas: 2
  selector:
    matchLabels:
      app: base-commerce
  template:
    metadata:
      labels:
        app: base-commerce
    spec:
      containers:
        - name: commerce
          image: 
